# 阶段约束（每次执行前必须查看）

## 通用约束

1. 整个过程在 `D:\AI\DawnNew` 文件夹操作，**不动原项目**
2. 每阶段开始前执行 `git init + git add . + git commit -m "阶段X"`
3. 重要文件复制前先备份
4. **零 Claude Code 残留**（品牌文字、import 路径、目录结构都不允许）
5. **全自构建**：每个文件都要理解和重写

## 当前阶段

根据对话进度自动更新。

## 质量要求

1. 所有 TypeScript 文件必须有完整类型定义
2. 核心引擎必须通过单元测试
3. 不引入未使用的依赖
4. 文件头标注模块职责
